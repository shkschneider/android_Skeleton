<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <style name="AppTheme" parent="@style/_AppTheme" />

    <style name="_AppTheme" parent="@style/Theme.AppCompat.Light.DarkActionBar">
        <!-- Material (thanks to AppCompat) -->
        <item name="colorPrimary">@color/actionBarBackgroundPrimaryColor</item>
        <item name="colorAccent">@color/contentForegroundHighlightColor</item>
        <!--<item name="colorControlNormal"></item>-->
        <!--<item name="colorControlActivated"></item>-->
        <!--<item name="colorControlHighlight"></item>-->
        <!--<item name="colorSwitchThumbNormal"></item>-->

        <!-- Styles -->
        <item name="actionBarStyle">@style/AppTheme.ActionBarStyle</item>
        <item name="actionModeStyle">@style/AppTheme.ActionModeStyle</item>

        <!-- Android -->
        <item name="android:windowBackground">@color/contentBackgroundColor</item>
        <item name="android:textColorPrimary">@color/contentForegroundPrimaryColor</item>
        <item name="android:textColorSecondary">@color/contentForegroundSecondaryColor</item>

        <!-- ... -->
    </style>

    <style name="AppTheme.ActionBarStyle" parent="@style/Widget.AppCompat.Light.ActionBar.Solid.Inverse" />

    <style name="AppTheme.ActionModeStyle" parent="@style/AppTheme.ActionBarStyle">
        <item name="background">@color/actionBarBackgroundPrimaryColor</item>
    </style>

</resources>
