<androidx.core.widget.NestedScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <include layout="@layout/content_shk_id" />

        <include layout="@layout/content_shk_code" />

        <include layout="@layout/content_shk_personal" />

        <include layout="@layout/content_shk_skills" />

        <View
            style="@style/SkeletonTheme.DividerHorizontal"
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_margin="@dimen/sk_spaceMicro" />

        <!-- FIXME drawableStart does not print icon on Nexus 5 API 28 -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/github"
            style="@style/SkeletonTheme.ButtonStyle.Inverted.Borderless"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="@dimen/sk_spaceTiny"
            android:drawableLeft="@drawable/ic_github"
            android:maxLines="2"
            android:minHeight="@dimen/sk_spaceMedium"
            android:text="@null"
            tools:ignore="RtlHardcoded" />

    </LinearLayout>

</androidx.core.widget.NestedScrollView>
